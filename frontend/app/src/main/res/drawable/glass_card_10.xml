<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Warm translucent glass -->
    <solid android:color="?attr/glassCardBackground" />

    <!-- Soft premium border -->
    <stroke
        android:width="1dp"
        android:color="?attr/glassCardStroke" />

    <!-- FIIE rounded geometry -->
    <corners android:radius="@dimen/fiie_card_radius" />

</shape>